{{ 'emails-removed_secret_santa.message.txt'|trans({'%name%': participant.name, '%owner%': participant.party.getOwnerName}, 'messages', locale)|raw }}

{{ 'emails-base_email_header.title'|trans({}, 'messages', locale) }}:

* {{ 'emails-base_email_header.date'|trans({}, 'messages', locale) }}: {{ participant.party.eventdate|format_datetime('medium', 'none') }}
* {{ 'emails-base_email_header.location'|trans({}, 'messages', locale) }}: {{ participant.party.location }}
* {{ 'emails-base_email_header.amount'|trans({}, 'messages', locale) }}: {{ participant.party.amount }}
* {{ 'emails-base_email_header.num_people'|trans({}, 'messages', locale) }}: {{ participant.party.participants|length }}
* {{ 'emails-base_email_header.person_created_list'|trans({}, 'messages', locale) }}: {{ participant.party.participants|first.name }} ({{ participant.party.participants|first.email }})

{{ 'emails-removed_secret_santa.btn.find_out'|trans({}, 'messages', locale) }}
{{ url('participant_view', { 'url': participant.url, '_locale': participant.party.locale }) }}

{{ 'emails-base_email_unsubscribe.unsubscribe' | trans({}, 'messages', locale) }} {{ 'emails-base_email_unsubscribe.unsubscribe_link' | trans({}, 'messages', locale) }} {{ url('unsubscribe_confirm', { 'url': participant.url, '_locale': participant.party.locale }) }}
