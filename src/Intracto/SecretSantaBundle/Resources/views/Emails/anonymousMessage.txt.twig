{{ 'emails-anonymous_message.message.txt'|trans({"%name%": entry.name , "%message%" : message | escape })|raw }}<br>
{{ url("participant_view", { "url": entry.url, "_locale": entry.pool.locale }) }}

