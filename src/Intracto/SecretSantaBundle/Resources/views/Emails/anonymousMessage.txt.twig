{% set emessage = message | escape %}
{{ 'emails-message.message.txt'|trans({"%name%": entry.name , "%message%" : emessage })|raw }}<br>
{{ url("entry_view", { "url": entry.url, "_locale": entry.pool.locale }) }}