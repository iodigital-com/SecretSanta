{{ 'emails-pool_status.message.txt'|trans({'%owner%': party.getOwnerName}) }}

{{ 'emails-pool_status.label.name'|trans }} >>
{{ 'emails-pool_status.label.email'|trans }} >>
{{ 'emails-pool_status.label.confirmed'|trans }} >>
{{ 'emails-pool_status.label.wishlist_filled'|trans }}

{% for participant in party.participants %}
    {{ participant.name }} >>
    {{ participant.email }} >>
    {% if participant.viewdate %}{{ 'emails-pool_status.yes'|trans }}{% else %}{{ 'emails-pool_status.not_yet'|trans }}{% endif %} >>
    {% if participant.wishlistItems is not empty %}{{ 'emails-pool_status.yes'|trans }}{% else %}{{ 'emails-pool_status.not_yet'|trans }}{% endif %}
{% endfor %}

{{ 'emails-pool_status.btn.poolstatus'|trans }}:
{{ url('party_manage', { 'listUrl': party.listurl }) }}
